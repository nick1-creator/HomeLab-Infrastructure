# Diagrams

This directory contains the public architecture diagram source (`architecture-overview.mmd`) and the rendered image (`architecture-overview.png`).

The main README already contains a Mermaid architecture diagram.
